"use client";

import React from "react";

const Grain = () => {
    return (
        <div className="fixed inset-0 pointer-events-none z-[100] noise-bg" aria-hidden="true" />
    );
};

export default Grain;
